# 技術債登記簿

每一項都必須可執行：有明確的驗收條件與最早可安全處理的日期。不收「TODO later」。

最後更新：2026-08-26

| ID | 問題 | 影響 | 優先 | 依賴 | 最早可處理 | 驗收條件 |
|---|---|---|---|---|---|---|
| TD-A1 | `@google/genai` 安裝 `^1.15.0`，最新為 `2.17.1`，差一個主版本 | 落後兩年份的 SDK 修正與型別；升級具破壞性風險 | 中 | Gemini production 穩定執行至少七天 | 2026-08-21 | 升 v2 後 `functions` typecheck 與全部 bot 測試通過，且 production smoke 重跑一次串流、取消、usage metadata |
| TD-A2 | `generateGeminiReply` 的 callable 層無測試（lease、replay、串流、取消、並行釋放） | 這些是 race-safety 的核心路徑，回歸不會被 CI 擋下 | **高** | 需要 Firestore 與 GenAI SDK 的 mock 邊界 | 立即 | 涵蓋首次請求、lease 中重複、過期 lease replay、完成後 replay、取消不留 final message、429/5xx/timeout、context > 24k、`finally` 釋放並行 |
| TD-A3 | `context-builder.ts` 與 `rate-limit.ts` 無測試 | context 修剪與速率限制錯誤只會在 production 顯現 | 高 | 同上 mock 邊界 | 已完成 | **2026-08-26 完成。** 新增 `functions/tests/helpers/firestore-fake.ts`（本 repo 第一個 Firestore mock 邊界，TD-A2 共用）、`functions/tests/rate-limit.test.ts`（23 例）與 `functions/tests/context-builder.test.ts`（30 例）。涵蓋排序與 tie-break、reply context、房間邊界、四個時間窗與各自上限、window 邊界換窗、逾期 lease 回收、同 runId 冪等、`finally` 釋放只清自己那筆，以及「任一窗爆掉就整筆交易不落地」。**測試有效性以變異驗證**，非僅綠燈：`DEFAULT_LIMIT 20→100`、拿掉來源訊息排除、拿掉 requester 檢查、放寬 widen 啟發式各被 3／18／1／3 例抓到；`userMinute 5→50` 與拿掉逾期 lease 過濾各被 3 例抓到。**過程中發現 TD-A6**（reply context 的「前半邊」查詢從未生效） |
| TD-A4 | Inline Grounding Citations（行內引用標註與文字 span mapping） | 訊息僅提供整則來源列表，無句級/段落級細緻引用 | 低 | Google Search Grounding 上線穩定 | 待排 | 保留 `groundingSupports`，支援點擊引文標號跳轉或反白對應來源 |
| TD-A5 | Dedicated Weather Tool（專用氣象 API 整合） | 搜尋 grounding 無法保證即時精確經緯度預報與警報結構 | 中 | Google Search Grounding 上線穩定 | 待排 | 整合專用 Weather API 提供氣溫、降雨機率、逐時/10日預報與卡片渲染 |
| TD-A6 | `context-builder.ts:78` 的 reply「前半邊」鄰近查詢從未生效：`orderBy('createdAt','desc').endAt(target).limit(5)` 在降冪下的結果集是「最新 → target」，`limit(5)` 取到的是**全房最新 5 則**，不是 target 前面 5 則 | reply 情境下模型看不到被回覆訊息「之前」的脈絡。取回的 5 則本來就落在最近視窗內，該查詢等於空操作。不影響授權或房間邊界，屬 context 品質缺陷 | 中 | 無 | 已完成 | **2026-08-26 修復。** 降冪往舊的方向取應為 `startAt(target)`；兩個 cursor 現在都錨在 target 上往外走，各含 target 本身再取四則。測試〈reaches the messages before the reply target (TD-A6)〉與〈walks five back and five forward from the target, and no further〉在修法前皆失敗、修法後通過，且後者刻意讓 before 側完全落在最近視窗之外，否則它會因為那些訊息本來就在視窗裡而假性通過——第一版就是這樣，已改掉。**同類搜尋**：全 repo 另外 7 處 cursor 全是 `startAfter(cursor)` 的同向分頁續讀，不存在第二個「把 `limit` 套在錨錯方向的掃描上」，已搜尋、無同類遺漏。發現於同日的 TD-A3 補測 |
| TD-C1 | PR #43 的三項 RTC 啟動最佳化（inline grant、App Check session warm-up、媒體與協商平行化）於 2026-08-16 部署，在 2026-08-17 之前沒有任何一組部署後的分段數字 | 無法判定是改善、無感或回歸。2026-08-15 的原結案把瓶頸歸給「Cloud Function 冷啟動」並宣告不要再改 client，該歸因已由 server 端對照推翻（暖機 server 端只佔 212 ms／2234 ms，9%），**不得再引用** | **高** | 需要兩個真實帳號、真實音訊裝置與 production | 已完成 | **2026-08-17 判定 `VERIFIED-IMPROVEMENT`（暖機）。** 七通實測（1 通 session 冷啟語音、5 通暖機語音、1 通視訊，`10:02:35Z–10:03:55Z`，client `4bb3ad7`／RTC Functions `aa646ac`）：暖機總計中位數 **5101 → 2334 ms（−54%）**，min/median/max 與逐階段對照見 `docs/RTC.md` 的 AFTER 表。假設 1 **CONFIRMED 且是結構性的**——`getLiveKitTokenV2` 在六小時窗內 server 端**零次呼叫**，該支已不在通話路徑上，不是變快而是消失；假設 3 **CONFIRMED**（`mediaReady` 1177 → 156 ms，−87%，而視訊仍需 1107 ms，代表該階段仍在量真正的媒體工作）；假設 2 **INCONCLUSIVE**，session 冷啟仍有 287 ms 的第一通溢價，缺 BEFORE 的同型對照，無法單獨歸因。**唯一變慢的階段是 `modulesReady` 11 → 37 ms（+26 ms）**，量級小、不改變判定，但不得從紀錄中略去。**冷啟動的 client 分段未取得**：該批七通的 server 全是暖的（`OPTIONS` 2.2–3.7 ms，冷啟為 1.5–2.9 s），因此 3559 ms 那通是 session 冷／server 暖，**不得拿去對照 BEFORE 的 18613 ms**——那樣會重複 2026-08-15 被推翻的同一種歸因錯誤。冷啟動僅有 server 端佐證：呼叫方成本 7379 → 4661 ms（−37%，量於 `10:01:32Z`），少掉的量級正好是 `getLiveKitTokenV2` 的獨立冷啟 |
| TD-C2 | 重連瞬間 presence heartbeat 會被 Rules 拒一次：`detected connection state mismatch` 後接 `update /realtime/presence/{uid}/connections/{id} failed: permission_denied` | console 被洗版，真正的授權問題會被藏在噪音裡 | 中 | 無 | 已完成 | **2026-08-17：根因已確認（CODE-PROVEN），與 TD-C3 同一個**，見下方「presence heartbeat 節點擁有權」。修法已隨 PR #47 合併並部署（hosting run `31995531725`，`4bb3ad7`）：`.info/connected` 為 false 時不再發出 heartbeat，因此重連時 SDK 不會 flush 一筆必被拒的排隊寫入。**驗收仍需真實斷網重測**：斷網 20 秒再連線，重連後不得出現 `permission_denied`；若仍出現一次且來自 server 已判定斷線、client 尚未察覺的半開連線，則記為 provider 行為並以 `NOT_APP_DEFECT` 結案（此時 heartbeat 會自行修復節點，presence 不得中斷）。 **2026-08-17 實測通過（操作者回報）。** 於部署後的 `4bb3ad7` 執行本列的驗收程序，結果為通過。**證據性質與 TD-C1 不同且必須寫明**：本列沒有隨附擷取紀錄，依據是操作者的實測回報。這類測試本來就只能由人在真實環境觀察，不存在可由終端重跑的等價 gate，因此「無擷取」是這類驗收的固有限制，不是本次的疏漏。若日後重連後再度出現 `permission_denied`，依本列既有的 `NOT_APP_DEFECT` 條款判讀。 |
| TD-C3 | 長時間開著的分頁寫入全部被拒：連續 28 次 heartbeat `permission_denied`，分頁齡 154 分鐘 | 使用者整天開著分頁會靜默從 presence／typing 消失，畫面卻仍顯示已連線 | 中 | 需要能重現：單一帳號、不換帳號、分頁放置 >1 小時 | 已完成 | **2026-08-17：根因已確認（CODE-PROVEN）並修復**，見下方「presence heartbeat 節點擁有權」；PR #47 已部署。**TEST B（換帳號，不長放）** 的程式路徑已釐清：`beginSession()` 先呼叫 `cleanupSession()`，而 `close()` 在第一個 `await` 之前就同步 `stopHeartbeat()` 並設 `closed`，因此舊 session 的 heartbeat 不可能在換帳號後繼續寫舊 uid 的節點——原本混在一起的兩個變因中，換帳號這一項在程式上不成立。**兩項實測仍要各自獨立執行並記錄**：TEST A 單一帳號、分頁放置 2 小時不切換帳號，presence heartbeat 全程不得讓使用者從他人清單消失；TEST B 登入後 5 分鐘內切換帳號，觀察切換後的 heartbeat。修法之後可接受的行為是「單次寫入失敗後立即自我修復」，不可接受的是「連續被拒且節點不再重建」。 **2026-08-17 實測通過（操作者回報）。** 於部署後的 `4bb3ad7` 執行本列的驗收程序，結果為通過。**證據性質與 TD-C1 不同且必須寫明**：本列沒有隨附擷取紀錄，依據是操作者的實測回報。這類測試本來就只能由人在真實環境觀察，不存在可由終端重跑的等價 gate，因此「無擷取」是這類驗收的固有限制，不是本次的疏漏。TEST A（單一帳號、分頁長放）與 TEST B（五分鐘內換帳號）各自獨立執行，兩者皆通過。 |
| TD-C4 | typing 指示器的 stale 清除路徑（`TYPING_STALE_AFTER_MS` 6 秒 ＋ `TYPING_SWEEP_MS` 2 秒 sweep）只有單元測試，沒有 production 異常終止實測 | 對方分頁被強制結束時 `onDisconnect` 不會執行，若 sweep 失效，A 會永遠停在「正在輸入…」 | 中 | 需要兩個真實帳號，且能對 B 製造真正的異常終止（工作管理員結束行程，不是正常關閉分頁） | 已完成 | B 開始輸入後強制結束其瀏覽器行程；A 的「正在輸入…」必須在約 8 秒內（6 秒 stale ＋ 最多一次 2 秒 sweep）消失。實測通過才可標記 `TYPING_STALE = VERIFIED-PRODUCTION`，否則維持 `MANUAL-VERIFICATION-REQUIRED`。 **2026-08-17 實測通過（操作者回報）。** 於部署後的 `4bb3ad7` 執行本列的驗收程序，結果為通過。**證據性質與 TD-C1 不同且必須寫明**：本列沒有隨附擷取紀錄，依據是操作者的實測回報。這類測試本來就只能由人在真實環境觀察，不存在可由終端重跑的等價 gate，因此「無擷取」是這類驗收的固有限制，不是本次的疏漏。據此標記 `TYPING_STALE = VERIFIED-PRODUCTION`。 |
| TD-C5 | `cleanupExpiredCallSignals` 每次排程執行都以 `FAILED_PRECONDITION` 失敗：`collectionGroup('incomingCalls').where('expiresAt','<=',…)` 是**單欄位 collection-group 查詢**，需要 `fieldOverrides` 的 COLLECTION_GROUP 例外，但 `firestore.indexes.json` 的 `fieldOverrides` 是空陣列 | `users/{uid}/incomingCalls` 的保留清理從未生效，文件無上限累積。通話流程本身不受影響（signal 仍可建立與讀取），屬成長／保留問題而非通話中斷 | 中 | 無 | 已完成 | **2026-08-17 已修復並部署。** PR #46 補上 `incomingCalls.expiresAt` 的 COLLECTION_GROUP 例外（同時列出原本自動建立的兩個 COLLECTION 排序，因為在 `fieldOverrides` 指名一個欄位是**取代**而非附加它的自動索引），新增 `firestore_indexes` 這個最小 rollout phase，並以 `scripts/index-contract.mjs` 把「查詢↔索引」契約變成 `pnpm test:unit` 的機器檢查。部署 run `31994462556`（`4eb21b4`）；COLLECTION_GROUP 索引於 `04:31:43Z` 轉為 READY；`04:32:26Z` 手動觸發 `firebase-schedule-cleanupExpiredCallSignals-asia-east1`，結果 `rtc.signal.cleanup / complete / count 0 / 372 ms`，無 `FAILED_PRECONDITION`。**剩下的驗收條件「過期 signal 確實被刪除」是時間閘，不是缺陷**：`SIGNAL_RETENTION_MS` 是 7 天，production 最舊一筆 signal 的 `expiresAt` 為 `2026-08-21T05:07Z`，因此 `count 0` 是正確答案，第一次真正的刪除最早於 2026-08-21 可觀測。**排程呼叫路徑本身也已驗證**：手動觸發只證明「查詢＋索引」可用，不證明 Cloud Scheduler 的呼叫路徑健康。`05:32`／`06:32`／`07:32`／`08:32`（UTC）四次非手動的自然週期全部為 `rtc.signal.cleanup / complete / count 0`，耗時 235–310 ms；該 service 自 `03:41:47Z` 最後一次 `FAILED_PRECONDITION` 之後，再無任何 WARNING 以上的紀錄 |
| TD-P3 | presence 的 12 小時 legacy 相容窗（`PRESENCE_LEGACY_TRUST_MS`） | 過渡期措施，長期會讓真正的殭屍連線多存活 12 小時 | 中 | 全部 client 都已載入 #33 之後的版本 | 已完成 | **2026-08-26 移除。** 兩份鏡像常數（`src/realtime/presence-state.ts`、`functions/src/presence/presence-policy.ts`）與各自的 `heartbeating` 分支一併刪除，讀取端與清理端都只剩 `PRESENCE_STALE_AFTER_MS`（135 秒）。**`updatedAt === connectedAt` 不再代表「舊版 client」**：節點在建立時本來就是這個形狀，而第一次心跳在 45 秒到來，遠在 135 秒窗內，因此剛連上的連線不需要特別待遇；同樣形狀在 135 秒後即判定為遺棄的 socket，這正是本列要回收的東西。測試改以 `unbeaten()` 命名並補上兩側邊界；`functions` 的「不得早於讀取端刪除」不變量現在對 beaten 與 unbeaten 兩種形狀各跑一次。**殘餘風險已知並接受**：若真有分頁自 PR #33 之前持續開著未重載（依 no-cache HTML 與已過的觀察期判斷，需連續逾 13 天），其擁有者會在他人清單上顯示離線，直到重載為止；這正是本列權衡的那一側 |
| TD-M1 | `membership.ts` 撤銷成員時只清 legacy room presence（`realtime/rooms/{key}` 下的 members／presence／typing／activity），未清 global `realtime/presence/{uid}` | 原記載為「被移除的成員在 global presence 仍可能顯示在線」。**該影響未能重現**，真正的缺陷在別處：線上清單的保證只靠一個沒有測試的過濾條件 | 中 | 無 | 已完成 | **2026-08-17 結案，但本列原本的處置是錯的，已撤回。** 三項 CODE-PROVEN：（1）**原處置「撤銷時一併移除 `realtime/presence/{uid}`」無效**——該節點的 `.write` 是 `auth.uid === $uid`，不含房間條件，而 `beat()` 失敗會呼叫 `establishOnce()` → `establish()` → `set()`，因此 server 端刪除會在一個心跳（≤45 秒）內被擁有者自己寫回；諷刺的是保證它寫回的正是本次收尾合併的 PR #47。（2）**原處置也有害**——該節點沒有房間維度，因撤銷 A 房而刪除它，會讓該使用者在他仍具成員資格的所有其他房間變成離線。（3）**原記載的影響未重現**——`revokeRoomMember` 的第一個 transaction 就把成員文件設為 `revoking`，而 `watchRoomMembers` 以 `status === 'active'` 過濾，因此被撤銷者在任何 realtime 清理發生**之前**就已離開清單。**實際修正的缺陷**：`onlineRoomMembers`（負責產生線上清單的投影函式）完全不看 `status`，整個保證只掛在另一個模組裡一行未經測試的 `.filter()` 上——刪掉那一行，被撤銷者立刻重新出現且顯示在線。已讓 `onlineRoomMembers` 自行檢查 `status === 'active'`（防禦縱深），並補上三層測試：`tests/presence-state.test.ts` 的投影測試（修正前失敗、修正後通過）、`tests/room-members.test.ts` 的訂閱過濾測試（以移除該 `.filter()` 的變異驗證其確實有效）、以及 `tests/rules.test.ts` 中一則規則測試，釘住「撤銷關閉 room mirror、但不影響 global presence 自寫權」——那正是（1）與（2）成立的原因。**同類搜尋**：RTDB 只有 `realtime/presence` 與 `realtime/rooms` 兩個子樹，typing／activity 皆為房間範圍且已隨撤銷清除；所有移除路徑（`revokeRoomMember`／`completeRevocation`／`reconcileMembership`／`reconcileMembershipMirrors`／`removeOrphanRealtimeAccess`）都收斂到 `removeRealtimeAccess*`，且不存在刪除房間或刪除帳號的路徑——**已搜尋，無同類遺漏**。另註：`realtime/presence/$uid` 的 `.read = auth != null` 是刻意設計，`tests/rules.test.ts` 既有斷言已涵蓋（跨使用者可讀、父層不可列舉），不屬本列缺陷 |
| TD-L1 | legacy RTC 三支（`startLiveKitCall`／`getLiveKitToken`／`endLiveKitCall`）仍部署 | 新舊雙架構並存，違反「不得永久 dual architecture」 | 中 | 七天觀察期，且確認無 supported client 流量 | 2026-08-21 | 依 production inventory 明確刪除該三支，HANDOFF 記錄 |
| TD-L2 | legacy `realtime/rooms/{roomKey}/presence` 路徑與其 rules 仍保留 | 同上 | 中 | 同上 | 2026-08-21 | 移除路徑與對應 rules，rules 測試更新 |
| TD-L3 | legacy push token 文件仍存在 | canonical registry 之外的殘留資料 | 低 | 同上 | 2026-08-21 | 確認 `pushTokenClaims` 為唯一來源後清除 |
| TD-U1 | `chat.controller.ts` **1496 行**（2026-08-19 實測；原記 1470，仍在增長），73 個頂層函式，同時管儲存、狀態、渲染、生命週期。耦合指紋：54 處 DOM 操作、41 個事件監聽、40 個訂閱 | 變更放大；渲染與訂閱耦合 | 中 | UI 方案選定 | UI Phase 3 | **原記的四模組拆法覆蓋不足，已修正**：該檔實際有 10 個職責群集，MessageRenderer／ComposerController／DrawerController／PresenceController 只涵蓋其中 3 個，通話、媒體語音、房間導航、session 生命週期、搜尋皆無歸屬。**2026-08-19 逐群集實測行數後再修正為 5 個模組**：media 僅 67 行、calls 98 行，依本列自訂的「不足百行併入相鄰模組」規則皆不獨立（media 併入 composer；calls 的實作已在 `call-ui.controller.ts` 的 398 行內，此處僅為轉接）。改拆為 MessageRenderer／**RoomController**（取代命名偏窄的 DrawerController）／ComposerController／PresenceController／SessionController。**真正的障礙不是分群而是狀態**：控制器有 36 個跨群集共用的模組層級 `let` 加 72 行 `ui` 物件，故每次抽出都必須明確帶走其狀態存取。**進度**：第 1 步已完成（PR #59），`src/app/message-view.ts` 收下 6 個零依賴純函式，控制器 1497 → 1447 行；`renderMessage` 一組需約 15 個依賴（含 setReply／startEditing／deleteMessage／joinCall／setReaction 等回呼），屬依賴注入改造，另案處理。詳見 [UI-PHASE-3](UI-PHASE-3.md) |
| TD-U2 | `src/style.css` 三層疊加（初版／2026 refinement／3.0 override），重複 media query，`.call-video` 重複定義 | 特異性債務，append-only 造成回歸 | 中 | UI 方案選定 | UI Phase 3 | **2026-08-19 完成（PR #57），但「無重複選擇器」只達成部分。** 已拆為 `src/styles/` 下的 tokens／base／layout／components／utilities 五層，`src/style.css` 僅保留依序 `@import`。重複選擇器 **102 組中合併 75 組，餘 27 組保留**（檔內仍有 32 組重複）——合併會把宣告移到最後一次出現的位置，可能越過它原本該輸的規則，**那會改變渲染**。每組合併前皆以「命中元素集合 × 設定屬性 × 特異性」判定；兩個實測回歸（≤720px 時 `.icon-btn` 勝過 `.composer-tool`；`.brand-mark` 蓋掉媒體查詢）由該分析事先預測。驗收依據：對照舊樣式表逐元素逐屬性比對，15 個視窗寬度、**4,088,550 次比對、0 差異**，且對著 app 實際載入的 `@import` 入口執行。詳見 [UI-PHASE-3](UI-PHASE-3.md) |
| TD-U3 | 死碼：`.typing-chip`／`.typing-dots`／`.typing-label`、`public/image/background.jpg`、`public/image/logo.png`（三者皆已核對為零引用）。**另有 `#chat-heads`，行數記載正確（`src/style.css` 297–364 共 68 行，涵蓋 `.chat-heads`／`.chat-head`／`-face`／`-badge`／`-name`／`-more` 與 `@keyframes chat-head-pop`），但它不是死碼**：`index.html:148` 有實際 DOM（`role="group" aria-label="未讀訊息懸浮氣泡"` 的 `hidden` 空容器） | 每次部署都在傳輸未使用的位元組 | 低 | 確認無動態引用 | UI Phase 3 | **2026-08-19 專案擁有者裁定：懸浮氣泡功能已放棄**，故 `src/style.css` 297–364 與 `index.html:148` 的 DOM **一併移除**（只刪 CSS 會留下無樣式的活元素）。其餘三項直接刪除。刪除後視覺與 build 驗證通過；**`@keyframes ring-pulse` 不可一併刪除**——它有四處使用（336／440／525／546），其中 **336 就在本次要刪的區塊內**（`.chat-head-badge::after`），刪後仍有 440／525／546 三處，故 keyframe 必須保留 |
| TD-U4 | `index.html` 共 **18 種非 ASCII 字形、23 處**（原記 13 種），零 SVG。但需分兩類：**UI 圖示 12 種**（U+00D7 ×4、U+FF0B ×3、U+2630、U+260E、U+25A3、U+2315、U+25CE、U+2726、U+25CF、U+263A、U+27A4、U+21AA）與**貼圖表情 6 種**（U+1F44B、U+1F499、U+1F602、U+1F389、U+1F44D、U+2615） | 各作業系統渲染尺寸與樣式不一致 | 中 | UI 方案選定 | UI Phase 3 | **只改 12 個 UI 圖示**為 inline SVG，`aria-label` 維持現狀。**6 個貼圖表情必須維持 Unicode 不變**——那是預設貼圖組的內容而非介面圖示，原記「圖示全為 Unicode → 全部改 SVG」若照字面執行會把貼圖組一併換掉。詳見 [UI-PHASE-3](UI-PHASE-3.md) |
| TD-D1 | `docs/motion.md:20` 引用不存在的 `chat-head-ping`（實際為 `chat-head-pop`） | 文件與程式碼不符 | 低 | 無 | 立即 | 修正或隨死碼一併移除 |
| TD-W1 | **移除成員沒有任何 UI 入口。** `revokeRoomMember` 已部署、rules 測試涵蓋、TD-M1 為它做過一整輪結案；client 也有包好的 `removeRoomMember()`（`src/rooms/room.service.ts:23`）——但**全 repo 沒有任何地方呼叫它**，`index.html` 的成員面板只列在線使用者，沒有管理操作 | 房主無法在 app 內移除成員，只能由 operator 直接呼叫 callable。這也解釋了為何 TD-C 系列的驗收條件一再要求「需要房間管理者」——那條路在 UI 上不存在 | 中 | UI 方案（成員面板要放什麼） | 立即 | 成員面板對房主顯示移除操作，走 `removeRoomMember`，含確認對話框與失敗處理；被移除者依既有 fail-closed 流程離開。發現於 2026-08-26 的死碼掃描：它是以「有 export 卻零呼叫」的形式浮現的 |
| TD-W2 | **使用者頭像照片存了但從不顯示。** `photoURL` 由 Google 登入取得並寫入 profile（`src/auth/profile.repository.ts:8`），但所有頭像一律 `textContent = initialOf(...)`，沒有任何路徑會在頭像裡放 `<img>` | 使用者看到的永遠是文字縮寫。存了一份沒有用途的個人資料，隱私面上是「收集了但不使用」 | 低 | UI 方案 | 立即 | 頭像有 `photoURL` 時渲染 `<img>` 並帶 `has-photo`；或確定不做，則連同 `profile.repository.ts` 的 `photoURL` 寫入一起移除，不要留著存而不用。**注意**：對應的 CSS（`.has-photo` 與 `.avatar img` 兩條）已於 2026-08-26 當作死碼移除，實作時需一併補回 |
| TD-W3 | `.call-message` 這個 class 由 `message.view.ts:231` 加上，但**從 PR #22 引入至今從未有任何 CSS 規則** | 通話系統訊息是唯一沒有辨識樣式的訊息種類，與相鄰的 `.sticker-message` 不對稱 | 低 | UI 方案 | 立即 | 補上樣式。**已確認不是 CSS 分層（PR #57）弄丟的**：分層前的 `src/style.css` 也搜不到它，因此不存在已上線的視覺回歸。刻意不在死碼掃描中刪除該 class emission，因為 UI 階段要用它 |
| TD-R1 | 媒體與貼圖批次（審計 PR 3）：共用 R2 primitives、貼圖管理 UI、URL 過期重取、上傳重試 | 使用者無法管理自訂貼圖；重複的 MIME／配額邏輯易漂移 | 中 | 無 | 待排 | 依審計 P1-16／P1-17／P2-07～09 逐項驗收 |
| TD-R2 | 備份與保留批次（審計 PR 5）：archive manifest、checkpoint、dry-run、restore runbook | 無法安全啟用資料保留 | 中 | 無 | 待排 | 依審計 P1-18 驗收；刪除預設維持關閉 |

## presence heartbeat 節點擁有權（TD-C2／TD-C3 的共同根因，2026-08-17）

兩列原本被當成兩個問題，實際上是同一個。根因為 CODE-PROVEN，由程式與 Rules 對讀得出，不是推測：

- heartbeat 送的是 partial update：`update(connection, { updatedAt: serverTimestamp() })`。
- `database.rules.json` 對該節點的 `.validate` 檢查的是**合併後**的結果：`!newData.exists() || newData.hasChildren(['state','connectedAt','updatedAt'])`。
- 節點存在時，合併結果有三個 child，通過；節點**不存在**時，合併結果只有 `updatedAt`，`hasChildren` 為 false，寫入被拒。

節點會在「這個分頁看不到任何狀態轉換」的情況下消失：server 一停止看到 socket 就執行 `onDisconnect`（此時 client SDK 仍可能回報 connected），而 `cleanupStalePresence` 會回收在背景分頁被節流、`updatedAt` 已過期的連線。兩種情況下 `.info/connected` 都沒有 false→true，所以唯一會重寫節點的 `establish()` 從不執行，之後每一次 heartbeat 都被拒——使用者對其他人來說整個 session 都是離線的，畫面卻仍顯示「已連線」。TD-C3 記錄的 28 次連續拒絕就是這個形狀。

另有一條獨立路徑通往同一個結果：`establish()` 在寫入前先 `stopHeartbeat()`，寫入失敗時沒有任何東西重啟它。原註解寫「下一次重連會重試」，但除非 socket 剛好再斷一次，否則沒有下一次。

修法（PR #47）是讓 session 擁有「節點是否存在」，而不只是擁有 socket 轉換：heartbeat 失敗即 `establishOnce()` 自我修復（且 `establish()` 會先重新 arm `onDisconnect` 才寫入，修復後的節點在真正斷線時仍會被回收）；`establish()` 失敗時仍在 `finally` 重啟 heartbeat；`.info/connected` 為 false 時不發 heartbeat。四個回歸測試在修法前全部失敗、修法後全部通過。

## 文件衝突（DOCUMENT-CONFLICT）

登記與 executable architecture 相牴觸的既有條目。程式碼、Rules 與設定優先於文件；未解決前不得依該條目施工。

| ID | 衝突條目 | 與什麼牴觸 | 為何 | 提出 |
|---|---|---|---|---|
| DC-1 | TD-M1「撤銷流程一併移除 `realtime/presence/{uid}`」 | `AGENTS.md` 條款 1（RTDB 不是授權來源）與 6（切房不得把使用者標為離線）、`docs/SECURITY.md`「Global Presence 不作為 room ACL」、`database.rules.json`（`realtime/presence/{uid}` 只由本人寫入，與 room membership 無關）、`src/realtime/presence-state.ts` 的 `onlineRoomMembers` | global presence 是「已登入的 session」，不是「某房間的成員」。因單一房間撤銷而刪除全域節點，會讓使用者在其他房間被誤判離線；該節點由 client 擁有寫入權，heartbeat 會立刻寫回，因此既不正確也無效 | 2026-08-15，Claude Code harness PR |
| DC-2 | TD-U4「圖示全為平台 Unicode 字形（13 種）」與驗收條件「改為 inline SVG」 | `index.html` 實際內容 | 實為 **18 種、23 處**，且其中 6 種（U+1F44B、U+1F499、U+1F602、U+1F389、U+1F44D、U+2615）是預設貼圖組的**內容**而非介面圖示。照原條目字面「全部改 SVG」會把貼圖組一併換掉。**本列危害最大**：它讀起來是可直接執行的清單，不會讓執行者察覺需要分類 | 2026-08-19 |
| DC-3 | TD-U3「**死碼**：`#chat-heads`」 | `index.html:148`（有實際 DOM） | 該元素**並非死碼**：`index.html:148` 有 `role="group" aria-label="未讀訊息懸浮氣泡"` 的 `hidden` 容器。照原條目只刪 CSS 會留下無樣式的活元素。功能存廢原本未記載，已於 2026-08-19 由專案擁有者裁定為放棄。**本列僅就「死碼」一詞成立；原記的「68 行 CSS」經複查為正確**（297–364 行整），2026-08-19 首次提報時誤記為 4 行，係只計含 `chat-heads` 複數字串的行、漏計 `.chat-head*` 單數家族所致，已更正 | 2026-08-19 |
| DC-4 | TD-U1「`chat.controller.ts` 已達 1470 行」與拆法「MessageRenderer／ComposerController／DrawerController／PresenceController」 | `src/app/chat.controller.ts`（實際 1496 行、73 個頂層函式） | 行數已過期且仍在增長。更重要的是該拆法只涵蓋檔案 10 個職責群集中的 3 個——通話、媒體語音、房間導航、session 生命週期、搜尋皆無歸屬，照字面執行無法達成本列自身「各有明確 ownership」的驗收條件 | 2026-08-19 |
| DC-5 | TD-U2 驗收條件「無重複選擇器」 | `src/styles/*.css` 的實際層疊行為 | 該條件在本檔上**無法完全達成**：102 組重複中有 27 組一旦合併就會改變渲染（合併會把宣告移到最後一次出現的位置，越過它原本該輸的規則）。已合併 75 組，餘下的保留並記錄原因。條件應改為「不得存在可安全合併而未合併的重複」 | 2026-08-19 |

TD-M1 的症狀前提也需重新確認：`chat.controller.ts` 的在線清單是 `onlineRoomMembers(members, onlineIds, selfUid)`，即 active room membership ∩ global presence，被撤銷者會因為離開 `members` 而消失。目前沒有任何 UI 直接呈現 global presence。正確驗收方向是「撤銷後該使用者不再出現在該房間在線清單，且其他房間在線狀態不變」，而非刪除全域 presence 節點。
